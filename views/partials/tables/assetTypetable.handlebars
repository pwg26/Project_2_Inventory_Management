<table id="type-assets" class="table table-striped">
    <thead>
        {{>tables/assetHeader}}
    </thead>
    <tbody>
        {{#each equipment}}
        {{>tables/assetTyperows}}
        {{/each}}
    </tbody>
</table>